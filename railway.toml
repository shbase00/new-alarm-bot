[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# IMPORTANT: Add a Volume in Railway dashboard
# Mount Path: /data
# Then add env var: DATABASE_PATH=/data/mints.db
